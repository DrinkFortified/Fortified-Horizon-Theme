{% render 'fortified-base' %}

<section
  id="FortifiedLandingHero-{{ section.id }}"
  class="fortified-landing-hero color-{{ section.settings.color_scheme | default: 'scheme-1' }}"
 style="--fortified-accent: {{ section.settings.highlight_color | default: '#1FA8A1' }}; text-align: {{ section.settings.content_alignment | default: 'center' }}; padding-block: {{ section.settings["padding-block-start"] | default: 40 }}px {{ section.settings["padding-block-end"] | default: 40 }}px; padding-inline: {{ section.settings["padding-inline-start"] | default: 0 }}px {{ section.settings["padding-inline-end"] | default: 0 }}px;">
  <div class="fortified-container fortified-landing-hero__inner">
    <div class="fortified-landing-hero__text">
      <h1 class="fortified-heading fortified-h1">
        <span class="fortified-landing-hero__line">{{ section.settings.headline_line_1 }}</span>
        <span class="fortified-landing-hero__line">{{ section.settings.headline_line_2 }}</span>
      </h1>
      <ul class="fortified-landing-hero__benefits" style="justify-content: {{ section.settings.blocks_alignment | default: 'center' }}; justify-items: {%- if section.settings.blocks_alignment == 'space-between' -%}stretch{%- else -%}{{ section.settings.blocks_alignment | default: 'center' }}{%- endif -%};">
        {% for block in section.blocks %}
          {% if block.type == 'fortified-benefit-item' %}
            <li class="fortified-landing-hero__benefit" style="text-align: {%- if block.settings.content_alignment == 'inherit' or block.settings.content_alignment == blank -%}inherit{%- else -%}{{ block.settings.content_alignment }}{%- endif -%};" {{ block.shopify_attributes }}>
              <span class="fortified-landing-hero__icon" aria-hidden="true">
                {% if block.settings.icon == 'star' %}★{% else %}✓{% endif %}
              </span>
              <span>{{ block.settings.text }}</span>
            </li>
          {% endif %}
        {% endfor %}
      </ul>
      {% if section.settings.cta_label != blank %}
        <a href="{{ section.settings.cta_url | default: '#' }}" class="fortified-btn fortified-btn--dark fortified-landing-hero__cta">{{ section.settings.cta_label }}</a>
      {% endif %}
      {% if section.settings.asterisk_disclaimer != blank %}
        <p class="fortified-landing-hero__disclaimer">{{ section.settings.asterisk_disclaimer }}</p>
      {% endif %}
    </div>
    <div class="fortified-landing-hero__media">
      {% if section.settings.image_1 != blank %}
        {{ section.settings.image_1 | image_url: width: 600 | image_tag:
            class: 'fortified-landing-hero__product fortified-landing-hero__product--1',
            widths: '300, 500, 600',
            sizes: '50vw',
            alt: '',
            loading: 'eager' }}
      {% endif %}
      {% if section.settings.image_2 != blank %}
        {{ section.settings.image_2 | image_url: width: 600 | image_tag:
            class: 'fortified-landing-hero__product fortified-landing-hero__product--2',
            widths: '300, 500, 600',
            sizes: '50vw',
            alt: '',
            loading: 'eager' }}
      {% endif %}
      {% if section.settings.image_3 != blank %}
        {{ section.settings.image_3 | image_url: width: 600 | image_tag:
            class: 'fortified-landing-hero__product fortified-landing-hero__product--3',
            widths: '300, 500, 600',
            sizes: '50vw',
            alt: '',
            loading: 'eager' }}
      {% endif %}
    </div>
  </div>
</section>

{% stylesheet %}
  .fortified-landing-hero {
    background: var(--color-background);
    color: var(--color-foreground);
    overflow: hidden;
    position: relative;
  }
  .fortified-landing-hero__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(24px, 4vw, 56px);
    align-items: center;
    min-height: clamp(440px, 52vw, 600px);
    max-width: var(--fortified-container);
    margin-inline: auto;
    padding-inline: clamp(16px, 3vw, 48px);
  }
  @media (min-width: 990px) {
    .fortified-landing-hero__inner { grid-template-columns: 1.1fr 1fr; }
  }
  .fortified-landing-hero__text {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 18px;
    text-align: left;
  }
  .fortified-landing-hero__text .fortified-heading {
    font-family: var(--font-fortified-display);
    font-weight: 900;
    text-transform: uppercase;
    font-size: clamp(44px, 7vw, 80px);
    line-height: 0.94;
    letter-spacing: -0.015em;
    margin: 0;
    color: var(--color-foreground-heading);
  }
  .fortified-landing-hero__line { display: block; }
  .fortified-landing-hero__line:nth-child(2) {
    color: var(--fortified-accent, var(--color-foreground-heading));
    font-style: italic;
  }
  .fortified-landing-hero__benefits {
    list-style: none;
    padding: 0;
    margin: 4px 0 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
    width: 100%;
    align-items: flex-start;
  }
  .fortified-landing-hero__benefit {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-size: 14px;
    font-weight: 500;
    line-height: 1.4;
  }
  .fortified-landing-hero__icon {
    width: 22px; height: 22px;
    border-radius: 50%;
    background: color-mix(in srgb, var(--color-foreground) 8%, transparent);
    color: var(--fortified-accent, var(--color-foreground));
    display: inline-flex; align-items: center; justify-content: center;
    font-size: 12px;
    font-weight: 700;
    flex-shrink: 0;
  }
  .fortified-landing-hero__cta {
    background: var(--color-primary-button-background);
    color: var(--color-primary-button-text);
    border: 1.5px solid var(--color-primary-button-border);
    border-radius: 999px;
    padding: 14px 32px;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    align-self: flex-start;
    margin-top: 8px;
    min-height: 48px;
    transition: transform .15s ease, filter .15s ease;
  }
  .fortified-landing-hero__cta:hover {  filter: brightness(1.1); }
  .fortified-landing-hero__disclaimer {
    font-size: 11.5px;
    opacity: 0.7;
    margin: 0;
    font-style: italic;
  }

  .fortified-landing-hero__media {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 320px;
  }
  .fortified-landing-hero__product {
    position: absolute;
    max-width: 240px;
    width: 35%;
    height: auto;
    object-fit: contain;
    filter: drop-shadow(0 20px 40px rgba(0,0,0,0.25));
  }
  .fortified-landing-hero__product--1 { transform: none; z-index: 1; }
  .fortified-landing-hero__product--2 { transform: none; z-index: 3; }
  .fortified-landing-hero__product--3 { transform: none; z-index: 2; }

  /* Decorative bubble shapes */

  @media (max-width: 749px) {
    .fortified-landing-hero__media { min-height: 280px; }
    .fortified-landing-hero__product { max-width: 180px; }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "LP Hero - Buy",
  "tag": "section",
  "class": "fortified-landing-hero-section",
  "max_blocks": 6,
  "settings": [
    {
      "type": "text",
      "id": "headline_line_1",
      "label": "Headline line 1",
      "default": "TRY 3 STICKS"
    },
    {
      "type": "text",
      "id": "headline_line_2",
      "label": "Headline line 2",
      "default": "FOR FREE*"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "CTA label",
      "default": "TRY 3 STICKS FOR $1.99"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "CTA URL"
    },
    {
      "type": "text",
      "id": "asterisk_disclaimer",
      "label": "Asterisk disclaimer",
      "default": "*Just pay shipping. Limited time offer."
    },
    {
      "type": "image_picker",
      "id": "image_1",
      "label": "Product image 1"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "Product image 2"
    },
    {
      "type": "image_picker",
      "id": "image_3",
      "label": "Product image 3"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "Content alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "select",
      "id": "blocks_alignment",
      "label": "Blocks row position",
      "info": "How the row/grid of blocks sits inside the section. Use Center to push blocks to the middle of the section width.",
      "options": [
        {
          "value": "flex-start",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "flex-end",
          "label": "Right"
        },
        {
          "value": "space-between",
          "label": "Space between"
        }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Padding top",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Padding bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding-inline-start",
      "label": "Padding left",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-inline-end",
      "label": "Padding right",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        {
          "value": "page-width",
          "label": "Page width"
        },
        {
          "value": "full-width",
          "label": "Full width"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color",
      "id": "highlight_color",
      "label": "Highlight / accent color",
      "info": "Used for accent bars, CTA backgrounds, checkmarks. Default: Fortified Teal.",
      "default": "#1FA8A1"
    }
  ],
  "blocks": [
    {
      "type": "fortified-benefit-item",
      "name": "Benefit item",
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            {
              "value": "checkmark",
              "label": "Checkmark"
            },
            {
              "value": "star",
              "label": "Star"
            }
          ],
          "default": "checkmark"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Premium Hydration + Performance"
        },
        {
          "type": "select",
          "id": "content_alignment",
          "label": "Block alignment",
          "options": [
            {
              "value": "inherit",
              "label": "Inherit from section"
            },
            {
              "value": "left",
              "label": "Left"
            },
            {
              "value": "center",
              "label": "Center"
            },
            {
              "value": "right",
              "label": "Right"
            }
          ],
          "default": "inherit"
        },
        {
          "type": "header",
          "content": "Spacing"
        },
        {
          "type": "range",
          "id": "padding-block-start",
          "label": "Padding top",
          "min": 0,
          "max": 100,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding-block-end",
          "label": "Padding bottom",
          "min": 0,
          "max": 100,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding-inline-start",
          "label": "Padding left",
          "min": 0,
          "max": 100,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding-inline-end",
          "label": "Padding right",
          "min": 0,
          "max": 100,
          "step": 2,
          "unit": "px",
          "default": 0
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "LP Hero - Buy",
      "category": "Fortified Landing",
      "settings": {},
      "blocks": [
        {
          "type": "fortified-benefit-item",
          "settings": {
            "text": "Premium Hydration + Performance"
          }
        },
        {
          "type": "fortified-benefit-item",
          "settings": {
            "text": "Zero Sugar, Zero Crash"
          }
        },
        {
          "type": "fortified-benefit-item",
          "settings": {
            "text": "Scientifically Dosed Ingredients"
          }
        }
      ]
    }
  ]
}
{% endschema %}
