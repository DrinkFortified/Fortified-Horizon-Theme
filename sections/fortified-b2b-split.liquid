{% render 'fortified-base' %}

<section
  id="FortifiedB2B-{{ section.id }}"
  class="fortified-b2b color-{{ section.settings.color_scheme | default: 'scheme-1' }}"
 style="--fortified-accent: {{ section.settings.highlight_color | default: '#1FA8A1' }}; text-align: {{ section.settings.content_alignment | default: 'center' }}; padding-block: {{ section.settings["padding-block-start"] | default: 40 }}px {{ section.settings["padding-block-end"] | default: 40 }}px; padding-inline: {{ section.settings["padding-inline-start"] | default: 0 }}px {{ section.settings["padding-inline-end"] | default: 0 }}px;">
  <div class="fortified-container fortified-b2b__inner">
    {% for block in section.blocks %}
      {% if block.type == 'fortified-b2b-card' %}
        <article class="fortified-b2b__card" style="text-align: {%- if block.settings.content_alignment == 'inherit' or block.settings.content_alignment == blank -%}inherit{%- else -%}{{ block.settings.content_alignment }}{%- endif -%};" {{ block.shopify_attributes }} style="--overlay: {{ block.settings.bg_overlay_opacity | default: 50 }}%;">
          {% if block.settings.image != blank %}
            {{ block.settings.image | image_url: width: 1200 | image_tag:
                widths: '600, 900, 1200',
                sizes: '(min-width: 750px) 50vw, 100vw',
                class: 'fortified-b2b__bg',
                alt: block.settings.heading,
                loading: 'lazy' }}
          {% endif %}
          <div class="fortified-b2b__content">
            {% if block.settings.eyebrow != blank %}
              <span class="fortified-eyebrow">{{ block.settings.eyebrow }}</span>
            {% endif %}
            {% if block.settings.heading != blank %}
              <h3 class="fortified-heading fortified-h3">{{ block.settings.heading }}</h3>
            {% endif %}
            {% if block.settings.body != blank %}
              <p class="fortified-b2b__body">{{ block.settings.body }}</p>
            {% endif %}
            {% if block.settings.cta_label != blank %}
              <a href="{{ block.settings.cta_url | default: '#' }}" class="fortified-btn fortified-btn--primary">{{ block.settings.cta_label }}</a>
            {% endif %}
          </div>
        </article>
      {% endif %}
    {% endfor %}
  </div>
</section>

{% stylesheet %}
  .fortified-b2b {
    background: var(--color-background);
    color: var(--color-foreground);
  }
  .fortified-b2b__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;
    max-width: var(--fortified-container);
    margin-inline: auto;
    padding-inline: clamp(16px, 3vw, 48px);
  }
  @media (min-width: 750px) {
    .fortified-b2b__inner { grid-template-columns: 1fr 1fr; }
  }

  .fortified-b2b__card {
    position: relative;
    overflow: hidden;
    border-radius: 18px;
    min-height: clamp(360px, 40vw, 480px);
    display: flex;
    align-items: flex-end;
    padding: clamp(24px, 3vw, 40px);
    isolation: isolate;
    background: color-mix(in srgb, var(--color-foreground) 12%, transparent);
    color: var(--color-background);
    transition: transform .35s ease;
  }
  .fortified-b2b__card:hover { transform: translateY(-4px); }
  .fortified-b2b__card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg,
      color-mix(in srgb, var(--color-foreground) 10%, transparent) 0%,
      color-mix(in srgb, var(--color-foreground) 75%, transparent) 100%);
    z-index: 0;
    pointer-events: none;
  }
  .fortified-b2b__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1;
  }
  .fortified-b2b__content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 12px;
    max-width: 90%;
  }
  .fortified-b2b__content .fortified-eyebrow {
    color: var(--color-background);
    opacity: 0.85;
    margin: 0;
  }
  .fortified-b2b__content .fortified-heading {
    color: var(--color-background);
    font-family: var(--font-fortified-display);
    font-weight: 900;
    text-transform: uppercase;
    font-size: clamp(24px, 3vw, 36px);
    line-height: 1.05;
    letter-spacing: -0.01em;
    margin: 0;
  }
  .fortified-b2b__body {
    font-size: clamp(13px, 1vw, 15px);
    line-height: 1.55;
    color: var(--color-background);
    opacity: 0.9;
    margin: 0;
    max-width: 42ch;
  }
  .fortified-b2b__content .fortified-btn {
    background: var(--color-background);
    color: var(--color-foreground);
    border: 0;
    padding: 12px 24px;
    border-radius: 999px;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-top: 4px;
    min-height: 44px;
  }
  .fortified-b2b__content .fortified-btn:hover {
    background: var(--fortified-accent, var(--color-foreground));
    color: var(--color-background);
  }
{% endstylesheet %}

{% schema %}
{
  "name": "09 \u2014 B2B Split",
  "tag": "section",
  "class": "fortified-b2b-section",
  "max_blocks": 2,
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "Content alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "select",
      "id": "blocks_alignment",
      "label": "Blocks row position",
      "info": "How the row/grid of blocks sits inside the section. Use Center to push blocks to the middle of the section width.",
      "options": [
        {
          "value": "flex-start",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "flex-end",
          "label": "Right"
        },
        {
          "value": "space-between",
          "label": "Space between"
        }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Padding top",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Padding bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding-inline-start",
      "label": "Padding left",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-inline-end",
      "label": "Padding right",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        {
          "value": "page-width",
          "label": "Page width"
        },
        {
          "value": "full-width",
          "label": "Full width"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color",
      "id": "highlight_color",
      "label": "Highlight / accent color",
      "info": "Used for accent bars, CTA backgrounds, checkmarks. Default: Fortified Teal.",
      "default": "#1FA8A1"
    }
  ],
  "blocks": [
    {
      "type": "fortified-b2b-card",
      "name": "B2B card",
      "settings": [
        {
          "type": "text",
          "id": "eyebrow",
          "label": "Eyebrow",
          "default": "THE TRUTH"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "BECOME A WHOLESALER"
        },
        {
          "type": "textarea",
          "id": "body",
          "label": "Body"
        },
        {
          "type": "text",
          "id": "cta_label",
          "label": "CTA label",
          "default": "JOIN NOW"
        },
        {
          "type": "url",
          "id": "cta_url",
          "label": "CTA URL"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Background image"
        },
        {
          "type": "range",
          "id": "bg_overlay_opacity",
          "label": "Overlay opacity",
          "min": 0,
          "max": 100,
          "step": 5,
          "default": 50,
          "unit": "%"
        },
        {
          "type": "select",
          "id": "content_alignment",
          "label": "Block alignment",
          "options": [
            {
              "value": "inherit",
              "label": "Inherit from section"
            },
            {
              "value": "left",
              "label": "Left"
            },
            {
              "value": "center",
              "label": "Center"
            },
            {
              "value": "right",
              "label": "Right"
            }
          ],
          "default": "inherit"
        },
        {
          "type": "header",
          "content": "Spacing"
        },
        {
          "type": "range",
          "id": "padding-block-start",
          "label": "Padding top",
          "min": 0,
          "max": 100,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding-block-end",
          "label": "Padding bottom",
          "min": 0,
          "max": 100,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding-inline-start",
          "label": "Padding left",
          "min": 0,
          "max": 100,
          "step": 2,
          "unit": "px",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding-inline-end",
          "label": "Padding right",
          "min": 0,
          "max": 100,
          "step": 2,
          "unit": "px",
          "default": 0
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "09 \u2014 B2B Split",
      "category": "Fortified Home",
      "settings": {},
      "blocks": [
        {
          "type": "fortified-b2b-card",
          "settings": {
            "heading": "BECOME A WHOLESALER",
            "cta_label": "JOIN NOW"
          }
        },
        {
          "type": "fortified-b2b-card",
          "settings": {
            "heading": "BECOME A PARTNER",
            "cta_label": "APPLY NOW"
          }
        }
      ]
    }
  ]
}
{% endschema %}
